// Import the test cases directly from the master problem definition!
const { tests } = require("../graph-dfs.js");

/**
 * Practice Session: Graph DFS — Number of Islands (topics/graphs)
 *
 * Implement BOTH recursive and iterative DFS from scratch.
 * Export whichever you want to test as `solution`.
 *
 * Re-code your solution from scratch below and run the tests to verify correctness!
 */
function numIslands() {
  // PRACTICE SOLUTION HERE

}

module.exports = {
  solution: numIslands,
  tests
};
